# 3D Self-Supervised JEPA Pre-training Schedule
epochs: 50
warmup_epochs: 5
batch_size: 4
learning_rate: 0.0005
min_lr: 0.00001
weight_decay: 0.05
optimizer: "adamw"
betas: [0.9, 0.95]
clip_grad_norm: 1.0
amp: true
save_freq: 10
eval_freq: 5
